export const map = ({ avatar_url: img = '', bio = 'Cannot get a biography.' } = {}) => ({
    // return { img: avatar_url, bio: bio };
    // return { img: avatar_url, bio };
        img,
        bio
});